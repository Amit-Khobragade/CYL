import 'package:flutter/material.dart';

class ColorVariables {
  static const navBarColor = Color.fromRGBO(234, 223, 223, 1);
  static const textPrimaryColor = Color.fromRGBO(95, 15, 64, 1);
  static const color60Per = Color.fromRGBO(242, 242, 242, 1);
  static const lowOpacityColor = Color.fromRGBO(255, 255, 255, 0.6);
  static const noOpacityColor = Colors.transparent;
  static const secondaryTextColor = Color.fromRGBO(116, 116, 116, 1);

  // ! AppBar
  static const appBarColor = Color.fromRGBO(240, 228, 222, 1);
  static const appBarShadowColor = Color.fromRGBO(0, 0, 0, 0.25);

//  ! Discounts
  static const discountPrimaryClr = Color.fromRGBO(222, 236, 240, 1);
}
